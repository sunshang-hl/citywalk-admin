node_modules
.DS_Store
dist
dist-ssr
*.local
.cursorrules

# Env files (keep only .env.example template)
.env
.env.*
!.env.example

# Auto-generated files
# auto-imports.d.ts / components.d.ts 入库：vue-tsc 先于 vite 生成它们运行，
# fresh clone 不入库会导致 pnpm build 失败（Cannot find name 'computed' 等）
.auto-import.json

# Local database export files
数据库文件/

# Temporary debug artifacts
.tmp-*
public/__probe__.html
